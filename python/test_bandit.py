# to invoke few bandit test plugin list
# https://bandit.readthedocs.io/en/latest/plugins/index.html#complete-test-plugin-listing

import subprocess

import requests

assert (2 + 2 == 5, "Banana we've got a problem")

exec('a = 47')


def lala(password="asdada"):
    pass


password = "mjhorangi"

while True:
  try:
    pass
  except Exception:
    continue

subprocess.Popen('/bin/ls *', shell=True)

requests.get('https://gmail.com', verify=False)

def open_ssl_socket(version=SSL.SSLv2_METHOD):
    pass
